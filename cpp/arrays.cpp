#include <bits/stdc++.h>
using namespace std;
// Get x's value from user at runtime, okay for a vector, illegal for an array
int listvector () {
    int x;
    cin >> x;
    int list_1[x];
    vector <int> list_2[x];
    return x;
}

int main() {
    return listvector();
    return 0;
}
